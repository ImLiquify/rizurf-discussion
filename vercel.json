{
  "regions": ["sin1"],
  "buildCommand": "npm run build",
  "functions": {
    "api/index.js": {
      "includeFiles": "dist/**"
    }
  },
  "crons": [
    { "path": "/cron/badges", "schedule": "0 18 * * *" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/index" }
  ]
}
